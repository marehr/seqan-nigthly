#!/bin/bash

. $(dirname $0)/setvars_64bit.sh

# This is interpreted by the gcc-wrapper scripts.
#export ARCH=i686

# For 32 bit builds, we need 32 bit versions of bzlib and zlib.
#export SEQAN_CMAKE_FIND_ROOT_PATH="/lib32;/usr/lib32"

